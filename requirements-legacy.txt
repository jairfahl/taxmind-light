# Dependências do módulo LEGADO (ui/admin.py — Streamlit).
# NÃO incluir no Dockerfile de produção.
# Instalar apenas para desenvolvimento local do painel admin legado.
#
# Uso: pip install -r requirements.txt -r requirements-legacy.txt
streamlit==1.35.0
